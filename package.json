{
  "name": "derivate",
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "packages/**"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna exec --scope='{@derivate/core,@derivate/io-ts-deriver,@derivate/example-tsc}' -- yarn build"
  }
}
